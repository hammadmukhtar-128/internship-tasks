# Flowdesk API (server)

Express + MongoDB backend. See the root `README.md` for full setup instructions.

Quick start:

```bash
cp .env.example .env
npm install
npm run dev      # starts on http://localhost:5000
npm run seed     # populates demo data (requires MongoDB running)
```
