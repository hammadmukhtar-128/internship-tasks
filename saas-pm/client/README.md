# Flowdesk (client)

React + Vite + Tailwind frontend. See the root `README.md` for full setup instructions.

Quick start:

```bash
cp .env.example .env
npm install
npm run dev      # starts on http://localhost:5173
```

Requires the backend (`../server`) to be running at the URL configured in `.env`.
