# Vite dev server proxies /api and /socket.io to the backend (see vite.config.js),
# so no env vars are strictly required for local development.
# If you deploy frontend and backend separately, set:
VITE_API_URL=http://localhost:5000
